<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="JustExtraLight.Views.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:applicationModel="using:Windows.ApplicationModel"
      xmlns:storage="using:Windows.Storage"
      xmlns:viewmodels="using:JustExtraLight.ViewModels"
      mc:Ignorable="d"
      Loaded="Page_Loaded">

    <Page.DataContext>

        <viewmodels:MainPageViewModel x:Name="mainPageViewModel" />

    </Page.DataContext>

    <Grid>

        <Grid.Resources>

            <DataTemplate x:Key="ImagesTemplate">

                <ItemContainer>

                    <Grid CornerRadius="5"
                          Height="200"
                          x:Name="AttachmentTemplateGrid"
                          Padding="5"
                          Width="200">

                        <Grid.Background>

                            <ImageBrush Stretch="UniformToFill" />

                        </Grid.Background>

                    </Grid>

                </ItemContainer>

            </DataTemplate>

            <Style BasedOn="{StaticResource AccentButtonStyle}"
                   x:Key="LocalButtonStyle"
                   TargetType="Button">

                <Setter Property="Height"
                        Value="50" />
                <Setter Property="HorizontalAlignment"
                        Value="Stretch" />

            </Style>

        </Grid.Resources>

        <Grid.RowDefinitions>

            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />

        </Grid.RowDefinitions>

        <TitleBar Background="{ThemeResource ControlFillColorDefaultBrush}"
                  Grid.Row="0"
                  Height="48"
                  x:Name="AppTitleBar"
                  Subtitle="BETA"
                  Title="Just eXtra Light">

            <TitleBar.IconSource>

                <ImageIconSource ImageSource="/Assets/Icons/JXLIco.ico" />

            </TitleBar.IconSource>

            <TitleBar.RightHeader>

                <Button Content="&#xE946;"
                        FontFamily="Segoe Fluent Icons">

                    <Button.Flyout>

                        <Flyout>

                            <Grid>

                                <Grid.ColumnDefinitions>

                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />

                                </Grid.ColumnDefinitions>

                                <Grid.RowDefinitions>

                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />

                                </Grid.RowDefinitions>

                                <Image Grid.Column="0"
                                       Grid.Row="0"
                                       Grid.RowSpan="2"
                                       Height="60"
                                       Margin="0,0,20,0"
                                       Width="60">

                                    <Image.Source>

                                        <BitmapImage DecodePixelHeight="60"
                                                     DecodePixelType="Logical"
                                                     DecodePixelWidth="60"
                                                     UriSource="/Assets/Icons/JXLIco.ico" />

                                    </Image.Source>

                                </Image>

                                <TextBlock Grid.Column="1"
                                           Grid.Row="0"
                                           Grid.RowSpan="2"
                                           HorizontalAlignment="Center"
                                           TextWrapping="Wrap"
                                           VerticalAlignment="Center">
                                    
                                    <Run FontWeight="SemiBold"
                                         x:Name="AppVersionRun" />
                                    
                                    <LineBreak />
                                    
                                    <Run x:Uid="AppMadeWith" />
                                    
                                    <Hyperlink NavigateUri="https://github.com/libjxl/libjxl">
                                        
                                        <Hyperlink.Inlines>
                                            
                                            <Run Text="libjxl" />
                                            
                                        </Hyperlink.Inlines>
                                        
                                    </Hyperlink>
                                    
                                    <Run FontSize="12"
                                         Text="(ver 0.12.0)" />

                                </TextBlock>

                            </Grid>

                        </Flyout>

                    </Button.Flyout>

                </Button>

            </TitleBar.RightHeader>

        </TitleBar>

        <Grid Grid.Row="1"
              MaxWidth="1500"
              Padding="36">

            <Grid.ColumnDefinitions>

                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />

            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>

                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />

            </Grid.RowDefinitions>

            <RadioButtons Grid.ColumnSpan="4"
                          HorizontalAlignment="Center"
                          IsEnabled="{x:Bind mainPageViewModel.AreRadioButtonsEnabled, Mode=OneWay}"
                          Margin="0,0,0,4"
                          MaxColumns="2">

                <RadioButtons.Items>

                    <RadioButton IsChecked="{x:Bind mainPageViewModel.ConvertToJXL, Mode=TwoWay}"
                                 x:Uid="ToJpegXL" />

                    <RadioButton x:Uid="FromJpegXL" />

                </RadioButtons.Items>

            </RadioButtons>

            <Expander Grid.ColumnSpan="4"
                      Grid.Row="1"
                      HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Stretch"
                      Margin="0,0,0,8">

                <Expander.Content>

                    <TextBox Margin="16"
                             x:Name="ArgumentsTextBox"
                             Text="{x:Bind mainPageViewModel.Arguments, Mode=TwoWay}"
                             x:Uid="ArgumentsTextBox" />

                </Expander.Content>

                <Expander.Header>

                    <StackPanel Orientation="Horizontal"
                                Padding="12">

                        <FontIcon FontSize="24"
                                  Glyph="&#xE756;"
                                  Margin="0,0,16,0" />

                        <TextBlock VerticalAlignment="Center">

                            <TextBlock.Inlines>

                                <Run x:Uid="ExpertsSection1" />
                                <LineBreak />
                                <Run FontSize="12"
                                     FontWeight="Light"
                                     x:Uid="ExpertsSection2" />

                            </TextBlock.Inlines>

                        </TextBlock>

                    </StackPanel>

                </Expander.Header>

            </Expander>

            <Button Command="{x:Bind mainPageViewModel.FolderCommand}"
                    Grid.ColumnSpan="2"
                    Grid.Row="2"
                    IsEnabled="{x:Bind mainPageViewModel.EnableAddButtons, Mode=OneWay}"
                    Margin="0,0,8,0"
                    x:Name="AddFolderButton"
                    Style="{StaticResource LocalButtonStyle}">

                <Button.Content>

                    <StackPanel Orientation="Horizontal">

                        <FontIcon Glyph="&#xE8B7;"
                                  Margin="0,0,8,0" />
                        <TextBlock x:Uid="AddFolderButton" />

                    </StackPanel>

                </Button.Content>

            </Button>

            <Button Command="{x:Bind mainPageViewModel.ImagesCommand}"
                    Grid.Column="2"
                    Grid.ColumnSpan="2"
                    Grid.Row="2"
                    IsEnabled="{x:Bind mainPageViewModel.EnableAddButtons, Mode=OneWay}"
                    x:Name="AddImagesButton"
                    Style="{StaticResource LocalButtonStyle}">

                <Button.Content>

                    <StackPanel Orientation="Horizontal">

                        <FontIcon Glyph="&#xE8B9;"
                                  Margin="0,0,8,0" />
                        <TextBlock x:Uid="ChooseImages" />

                    </StackPanel>

                </Button.Content>

            </Button>

            <Button Command="{x:Bind mainPageViewModel.ConvertImagesCommand}"
                    Grid.ColumnSpan="4"
                    Grid.Row="3"
                    Height="60"
                    HorizontalAlignment="Stretch"
                    IsEnabled="{x:Bind mainPageViewModel.EnableConvertButton, Mode=OneWay}"
                    Margin="0,8"
                    x:Name="ConvertButton">

                <Button.Content>

                    <StackPanel Orientation="Horizontal">

                        <FontIcon Glyph="&#xE895;"
                                  Margin="0,0,8,0" />
                        <TextBlock x:Uid="ConvertButton" />

                    </StackPanel>

                </Button.Content>

            </Button>

            <Button Command="{x:Bind mainPageViewModel.SaveImagesCommand}"
                    Grid.ColumnSpan="3"
                    Grid.Row="4"
                    HorizontalAlignment="Stretch"
                    IsEnabled="{x:Bind mainPageViewModel.EnableSaveButton, Mode=OneWay}"
                    Margin="0,0,8,0"
                    x:Name="SaveImagesButton">

                <Button.Content>

                    <StackPanel Orientation="Horizontal">

                        <FontIcon Glyph="&#xE78C;"
                                  Margin="0,0,8,0" />
                        <TextBlock x:Uid="SaveImagesButton" />

                    </StackPanel>

                </Button.Content>

            </Button>

            <Button Command="{x:Bind mainPageViewModel.ClearImagesCommand}"
                    Grid.Column="3"
                    Grid.ColumnSpan="1"
                    Grid.Row="4"
                    HorizontalAlignment="Stretch"
                    IsEnabled="{x:Bind mainPageViewModel.EnableClearButton, Mode=OneWay}"
                    x:Name="ClearListButton">

                <Button.Content>

                    <StackPanel Orientation="Horizontal">

                        <FontIcon Glyph="&#xE894;"
                                  Margin="0,0,8,0" />
                        <TextBlock x:Uid="ClearListButton" />

                    </StackPanel>

                </Button.Content>

            </Button>

            <TextBlock Grid.Column="0"
                       Grid.ColumnSpan="4"
                       Grid.Row="5"
                       HorizontalAlignment="Center"
                       x:Name="DragAndDropText"
                       Style="{StaticResource TitleTextBlockStyle}"
                       TextAlignment="Center"
                       VerticalAlignment="Center"
                       x:Uid="DragAndDropInstructions"
                       Visibility="{x:Bind mainPageViewModel.ShowListText, Mode=OneWay}" />

            <ItemsView AllowDrop="True"
                       Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}"
                       DragOver="{x:Bind mainPageViewModel.ImageItemsView_DragOver}"
                       Drop="{x:Bind mainPageViewModel.ImageItemsView_Drop}"
                       Grid.Column="0"
                       Grid.ColumnSpan="4"
                       Grid.Row="5"
                       ItemsSource="{x:Bind mainPageViewModel.ImagesList}"
                       Margin="0,8,0,0"
                       x:Name="ImageItemsView"
                       SelectionMode="None">

                <ItemsView.ItemTemplate>

                    <DataTemplate x:DataType="viewmodels:ImageInfo">

                        <ItemContainer>

                            <Image Height="72"
                                   Width="72">

                                <Image.Source>

                                    <BitmapImage DecodePixelHeight="72"
                                                 DecodePixelType="Logical"
                                                 DecodePixelWidth="72"
                                                 UriSource="{x:Bind TemporaryPath}" />

                                </Image.Source>

                            </Image>

                        </ItemContainer>

                    </DataTemplate>

                </ItemsView.ItemTemplate>

                <ItemsView.Layout>

                    <UniformGridLayout ItemsStretch="Uniform"
                                       MinColumnSpacing="4"
                                       MinRowSpacing="4" />

                </ItemsView.Layout>

            </ItemsView>

            <StackPanel Background="{ThemeResource AccentAcrylicInAppFillColorDefaultBrush}"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Grid.Column="3"
                        Grid.Row="5"
                        HorizontalAlignment="Right"
                        Margin="8"
                        Orientation="Horizontal"
                        Padding="12,8"
                        VerticalAlignment="Bottom">

                <ProgressRing Height="24"
                              IsActive="{x:Bind mainPageViewModel.IsConversionInProgress, Mode=OneWay}"
                              Margin="0,0,8,0"
                              x:Name="LoadingRing"
                              Visibility="{x:Bind mainPageViewModel.ShowProgressRing, Mode=OneWay}"
                              Width="24" />

                <TextBlock Style="{StaticResource BodyLargeStrongTextBlockStyle}"
                           Text="{x:Bind mainPageViewModel.ImagesCount, Mode=OneWay}" />

            </StackPanel>

        </Grid>

        <InfoBar Grid.Row="1"
                 IsOpen="{x:Bind mainPageViewModel.EnableSaveButton, Mode=OneWay}"
                 Message="{x:Bind mainPageViewModel.InfobarMessage, Mode=OneWay}"
                 Title="{x:Bind mainPageViewModel.InfoBarTitle, Mode=OneWay}"
                 Severity="{x:Bind mainPageViewModel.Severity, Mode=OneWay}"
                 VerticalAlignment="Bottom" />

    </Grid>

</Page>